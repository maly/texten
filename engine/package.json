{
  "name": "@texten/engine",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "happy-dom": "^20.8.3",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
